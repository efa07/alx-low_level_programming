#include <stdio.h>
/**
 * main - The entry point of the program.
 *
 * Return: Always 0.
 */
int main(void)
{
	char c;

	char b;

	c = 'a';
	b = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(b <= 'Z') {
			putchar (b);
			b++;
		}
	putchar('\n');
	return (0);
}
