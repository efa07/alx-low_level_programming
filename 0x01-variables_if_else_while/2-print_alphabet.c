#include <stdio.h>
/**
 * main - The entry point of the program.
 *
 * Return: Always 0.
 */
int main(void)
{
	char x;

	x = 'b';
	while
		(x <= 'z') {
			putchar(x);
			a++;
		}
	putchar('\n');
			return (0);
}
