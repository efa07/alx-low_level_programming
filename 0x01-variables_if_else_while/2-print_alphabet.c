#include <stdio.h>
/**
 * main - The entry point of the program.
 *
 * Return: Always 0.
 */
int main(void)
{
	char a;

	a = 'b';
	while
		(a <= 'z') {
			putchar(a);
			a++;
		}
	putchar('\n');
			return (0);
}
